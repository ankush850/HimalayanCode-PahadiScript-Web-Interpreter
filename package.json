{
  "name": "himalayancode-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root scripts proxy for PahadiScript IDE",
  "scripts": {
    "dev": "npm --prefix landing run dev",
    "build": "npm --prefix landing run build",
    "start": "npm --prefix landing run start",
    "install-frontend": "npm --prefix landing install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.107.0"
  }
}
